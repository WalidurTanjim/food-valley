const SectionTitle = ({ title }) => {
    return (
        <div className="text-center">
            <h1 className="text-2xl md:text-3xl lg:text-4xl font-medium text-slate-700">{title}</h1>
        </div>
    );
};

export default SectionTitle;